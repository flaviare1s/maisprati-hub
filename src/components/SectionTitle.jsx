export const SectionTitle = ({ title }) => {
  return (
    <h2 className="uppercase font-lg text-light bg-blue-logo py-2 px-6 rounded-full w-[90%] sm:max-w-[500px] m-5 text-center font-bold">{title}</h2>
  )
}
