export const SectionTitle = ({ title }) => {
  return (
    <h2 className="uppercase font-lg text-white bg-red-secondary py-2 px-6 rounded-full w-full sm:max-w-[500px] m-5 text-center font-bold">{ title }</h2>
  )
}
